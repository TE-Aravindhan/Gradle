package com.smartek.groovydemo

class Students {
	
	def name, id, standard
	
	void set(id) {
		
		if(id!=null) {
			this.id = id
		}
		throw new IllegalArgumentException("Id can not be null")
	}
	
}


s = new Students(name:'aravind', standard:12)

println s.name +" "+ s.id+" "+s.standard